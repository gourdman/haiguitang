PORT=3001
# 前端地址（可用逗号分隔多个）：
# CORS_ORIGIN=http://localhost:5173

# DeepSeek API
# 服务端从 backend/.env 读取；若无该文件则读取本文件（.env.example）。勿将真实 Key 提交到 Git。
# 可选：deepseek-chat / deepseek-reasoner
DEEPSEEK_MODEL=deepseek-chat
# 可选：自定义超时（毫秒）
DEEPSEEK_TIMEOUT_MS=30000

